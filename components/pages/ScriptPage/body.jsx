export function body(imports, props, styleDefault, Root = "main") {
	const {
		NavigationWrapper,
		HorizontalSeparator,
	} = imports
	return (
		<NavigationWrapper>
			<HorizontalSeparator />
		</NavigationWrapper>
	)
}
