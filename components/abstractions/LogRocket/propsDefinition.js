export function propsDefinition(PropTypes) {
	/* Example:
  return {
    title: {
      propType: PropTypes.string.isRequired,
      defaultProp: "Some value",
    },
    keywords: {
      propType: PropTypes.string,
    }
  }; */
}
